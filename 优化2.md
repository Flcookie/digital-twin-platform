# 本周老师反馈整理（优化 2）

> 依据本周当面沟通语音转写整理（有错字、口语重复处已按语义合并）。  
> 对应实现主要在 `streamlit_app/ui_kpi_display.py`、`ui_part_trace_panel.py`、`ui_home.py`、Digital Twin / 上传相关页面等。

---

## 〇、本周核心工作方式（老师反复强调，优先执行）

| 原则 | 老师原意 |
|------|----------|
| **先样式、后逻辑** | 不要每次来又改逻辑又改样式，容易混在一起搞不清。先把**样式/layout 定死**，再只改逻辑。 |
| **先布局、后配色** | 先把东西放哪、多大、对齐定好；颜色、装饰是「顶上添花」，布局没好会顾此失彼。 |
| **先定一屏、再扩** | 一个页面样式定下来后尽量别再大动；逻辑可以迭代，样式反复改很耗时间。 |
| **功能渐进** | 不必一次塞满所有功能；未完成的用**整块 disable（decouple）** 或按钮灰掉，函数先不写。 |
| **简洁（奥卡姆）** | 没必要的信息、标签、单位重复、图例、竖线、多余日期文字——能删就删。 |

**老师建议的执行顺序：**

1. 布局（区块、宽度、字体层级、对齐）  
2. 标题与单位规范（标题 + 括号单位，图内不重复标单位）  
3. 配色（可用框架 colormap / 定一套绿蓝红黄灰）  
4. 图标（最后考虑，能统一 emoji/一套资源就统一）  
5. 逻辑与交互（checkout 变色、排序、上传流程等）

**你计划：** 今晚/回去先按今天说的改样式；明天早上再来对逻辑。

---

## 一、实验室与生活（非代码）

- 实验室一般 **21:00 前** 走较正常；小门 21:00 后不好开。  
- 21:00 后若必须离开：小门旁**变电箱**处有人爬过（有脚印），老师自己也爬过——仅作信息，注意安全与规定。  
- 钥匙：本周想调钥匙相关（具体未展开）。  
- 老师明天也会在实验室；你可自行改完再来。  
- **进度期望（约 2 个月 / 到 6 月底）**：至少 **三个页面** 界面 + **基本逻辑** 跑通；simulation 等后面再议。老师无法替你细想全部业务逻辑，但能帮你看是否合理。  
- 你 **6 月中旬有两场期末考试**，需自己把握节奏；测试在公司常专人做，一人开发就「用着修 bug」。

---

## 二、KPI / 图表区（Realtime 或 KPI 子页）

### 2.1 指标命名与含义（纠正与统一）

| 话题 | 老师说法 |
|------|----------|
| **Lead Time / Last N** | 你写的「Last N of operations」老师没懂；**不是 observation time**。上面已有各站信息时，**单看 Lead Time 意义不大**。 |
| **两个数一起展示** | 记得上周说过要 **两个指标一起** 看；当前把两值摆出来若是「损失」类，**只展示没太大意义**——要想清楚展示目的。 |
| **是否平均** | 有的不是平均，也是损失类；命名要统一，别和 WIP/AVG 混用。 |
| **Observation number** | 老师不确定是不是这个叫法；单位、命名要你自己查清楚再标。 |
| **WIP** | 直接叫 **WIP**；平均的叫 **AVG WIP**（或统一 `AVG` 前缀），空间够就写全，别有的地方 AV 有的地方又不放。 |
| **时间** | 一处标了 **seconds** 就别在另一处重复；**seconds** 与 **rate** 是两类东西，不要标乱。 |
| **Rate 这个词** | 老师觉得叫 **rate** 有点怪、易产生歧义；**probability** 太绝对（像「发生非裂的概率」）。可考虑：**fraction**、**idle fraction** 等；**bitrate** 提过又算了。 |
| **完成一圈的时间** | 每完成一圈显示时间——你忘了上次怎么改；老师也没完全复述清，需你对照上周笔记/代码。 |
| **「五」/跑起来** | 上次关于「五」、跑起来后某显示是什么，双方都有点记不清，需你对照旧版。 |

### 2.2 折线图：两根线（重点确认）

- 老师记得上周是 **两根线**，不是三张图各一对；也曾想过 stage 相关 support，后来倾向 **展示损失/完成类指标** 要简单清晰。  
- **Lead Time 折线**：单独意义不大；更有用的是随时间的：  
  - **Completion rate**  
  - **Scrap rate**  
  - 横轴 **时间** 即可。  
- 两根线要 **同时存在**（不要只剩一根或和上周不一致）。  
- **Y 轴**：尽量 **从 0 开始**；若前面有一段「切下来」没有点，是等间隔/采样导致，可接受，但要想是否误导。  
- **竖虚线 / 某日期文字（如 3/17）**：老师倾向 **去掉**——大家已懂，图上保留 **时间、WIP、AVG** 三要素即可；数值若已在 hover/线上标了，额外文字可减。  
- **Over time**：标题里 **不必写 "over time"**，一看折线图就懂。  

### 2.3 单位与标题格式（统一规范）

老师希望全项目统一一种标法：

```
标题（单位）
```

- 例：`WIP (count)`、`Completion & Scrap (%)`、`Pieces` / `Parts` —— **图内曲线不再标单位**。  
- **WIP、number 类**：可不加单位或括号里写 count。  
- **百分比图**：Y 轴可直接 **0–100** 标 `%`，与下面子图一致；下面标了 `%` 上面就别再各写一套。  
- **上面那张图**（双率）：单位难标在轴旁——用 **标题括号** 标 `%`；**不要** 在图内再写 `0.0` 那种和百分比混用的刻度（你这次数据可能没有小数率，检查是否用错 scale）。  
- **Pieces**：可能用 **pieces** 比 parts 多；你们场景也可叫 **parts**（8 parts）；和 PAT 等缩写统一即可。  
- **对称**：上下两张图风格、边距、是否标数字，尽量对称。  

### 2.4 颜色与 stage 条

- **Stage logging / 条形色**：老师认为 **可能都不用颜色**，现有颜色「没什么用」；若保留，需和 **上面状态条颜色一致**。  
- **顺序**：例如 **WIP → AVG WIP → Flow time → 各 stage**（每个 stage 是否写全看位置够不够）。  
- **下面 rotation 相关命名**：和前面 KPI 叫法 **容易混淆**（station / 专业叫法 / 你写的普逼几等）——要简化、统一。  
- **状态色建议（若保留）** — 语音转写曾记成「Busy 黄 / Idle 绿」，按老师语义与 `优化2-执行计划.md` 版式定稿 **以执行为准**：  
  - **Busy**：绿（正常在干活）  
  - **Idle**：灰（不动）  
  - **Blocked**：黄  
  - **Fail**：红  
  - **Log / 静止**：灰  
  - **一开始就要定好配色**，别后补；与上面 State 条、Station 堆叠条同色源（`ui_theme.STATUS_COLORS`）。  
- 参考：**上面一块 + 横线分割 + 下面图**（师弟那种），标题层级少而一致；**Current** 可改 **State**。  

### 2.5 图例、hover、视觉

- **中间点上的图标/图例**：不需要；能 **disable 图例** 或只保留必要 hover。  
- Hover 应显示 **有用信息（如 part）**，不要让人猜 **绿色是什么意思**（你参考了小组同学样式，但 **占视觉太大**）。  
- 字体大小、上下区块 **变化太大** 会显得怪——统一字号层级。  

---

## 三、Digital Twin / Part Trace / 状态展示

### 3.1 命名简化

- 不要过长、绕：**Past trace** / **Trace** / **Location** 即可。  
- 「经过的所有 log」是否包含 Transfer 等——当时设计就不清晰，**命名要帮用户一眼懂**。  
- Transfer 在 comp 里没放等问题，属于 **设计说明**，做细了再定，老师认为这块「没那么重要但做得很细」。  

### 3.2 轨迹 hover 与宽度

- **很重要**：鼠标放在轨迹上要看得到信息；你现在 **放上去看不到** 是硬问题。  
- 能否 **加宽** trace 区域：可以和前面布局统一，也可略扩；界面你觉得乱就先收，但 **hover 可读** 优先。  

### 3.3 图标

- 现在图标像 **不是同一套**（像 emoji / 不同包混用）；能换就换，**直接用他们那套表情/图标资源** 更省事（格子自适应）。  
- 不必自己画很多张图，**一套统一** 即可。  

### 3.4 Checkout 排序与变色（逻辑+样式）

- **Checkout 列表**：建议能 **sort**，新 part 进来后 **相对顺序稳定**（不要 Pat6 进来 avatar 乱跳）。你演示时顺序交换了，要弄清是 bug 还是设计。  
- **变绿**：checkout 后 **一直绿到下一轮** 老师觉得怪——checkout 应 **离开**；建议 **checkout 绿一下再恢复**，scrap **红一下**；或 **不做变色** 更简单。  
- 整体 **有点乱**，可简化交互。  

### 3.5 初始化与样式不一致

- **未跑时** 和 **跑起来后** 样式差太大，像 **跑起来才初始化**——应该 **启动前就初始化好布局/样式**，运行中只 **填数据**；最多多一行、闪一下绿/红表示更新。  
- 表格样式曾调过又 **弹回旧样式**，说明 **初始化顺序** 或 **session 重置** 有问题。  
- **对齐**：控件 **一大一小**（「全变成」）很奇怪，要统一尺寸。  

### 3.6 Live vs History 颜色

- **Live** 轨迹：老师倾向 **红色** 比绿色合适（绿色像 history）。  
- 具体你再斟酌，原则：**live / history 要区分且别抢眼到乱**。  

---

## 四、主控 / Home / 禁用与 Recall

### 4.1 整块禁用（Decouple）

- 切换模式、未就绪时：**整块 panel decouple 掉**（不可见/不可点），不要 **每个按钮单独变灰**——像 Cabe 掉整块，框架应自带能力。  
- 你 recall/准备时：后台读文件会先 **灰一下**——老师认为点选时 **相关区域应一起灰掉**，体验才一致。  

### 4.2 上次表格 vs 现在

- 上次用大 **表格** 展示控制，老师 **吃不下**、信息过载。  
- 现在拆条了更好，但 **样式要先定版** 再堆逻辑。  

### 4.3 上传 / 选文件流程

- 当前：**选完似乎直接 upload**，老师觉得 **怪**；常见是 **选本地 → 再确认是否上传/替换**。  
- 需求：上传后若要换文件，应有 **取消/重选**；是否进数据库、是否有 **delete**，要想清楚。  
- 上传对不对很难判，正规会有 **hash 校验**（老师不做前端，仅提示）。  
- 布局：上传区可 **小一点**，和上面控件 **同一行高度**，约 **占 1/3 宽**（三列之一）；多余 **身子/空白** 可收紧；字体略有色也行，非必须。  

### 4.4 钥匙与间距

- 钥匙种类 **两三种太多**；**B 键** 与相邻间距应和别的 **等价对齐**。  
- 你曾问 GPT 排布局又自己重排——老师认可 **可参考**，但最终以 **对齐、层级** 为准，不是堆颜色。  

---

## 五、设计原则备忘（老师归纳）

1. **样式统一**：上下同宽、同字体、同标题格式、同单位写法。  
2. **简洁**：KPI 已有 WIP 字样就不要再标「密补」之类重复；单位在标题写一次即可。  
3. **布局 > 配色 > 图标**；配色可用 **colormap** 选一套，再定「绿=idle、红=fail…」。  
4. **字体分层级**：第几层标题、按钮多大、是否粗体——定一套全站复用。  
5. **主观自评**：改完自己看一眼是否乱；百分比放轴上还是仅标题，没有唯一标准，以 **不乱、不重复** 为准。  
6. **别一次做太多**：有骨架后 **一个功能一个功能** 加；否则测试组合爆炸，你也难维护。  

---

## 六、待你自行核对/补全的模糊点

| 项 | 说明 |
|----|------|
| 上周「五」、跑起来显示 | 双方记忆模糊，需翻上周记录或 git。 |
| 每圈完成时间 / 损失怎么改 | 你忘了具体改法，需对照 `kpi_calculator` / 趋势序列。 |
| Last N operations  exact 定义 | 老师不懂该字段；需文档化或改名。 |
| 三根线 vs 两根线、stage 图是否保留 | 倾向两率折线 + 简化 stage 条，最终以你复盘上周为准。 |
| 点 KPI 连 log 联动 | 老师觉得「看不出来」，你有整理逻辑明天带来对。 |
| Simulation 细节 | 本月不优先，基本点击不错为先。 |

---

## 七、建议任务清单（按老师优先级）

### P0 — 本周先做（样式）

- [ ] 全站：**标题（单位）**、图内只标曲线名/数值，去掉重复单位与多余竖线、日期注记。  
- [ ] KPI 趋势图：**Completion rate + Scrap rate** 双线、时间轴、Y 从 0、% 轴统一。  
- [ ] 上下图 **宽度/字体/分割横线** 与师弟参考样式对齐。  
- [ ] 状态条：改名 **State**；色板定稿（busy/idle/fail/log）。  
- [ ] 控件 **等大对齐**；未实现功能 **整块 decouple**。  
- [ ] Trace：**hover 可见**；图标换统一一套；命名 **Trace / Past trace**。  

### P1 — 样式定版后（逻辑）

- [ ] Checkout 排序稳定；checkout/scrap **短暂变色或取消变色**。  
- [ ] **启动前** 完成 UI 初始化，避免跑起来才「变样」。  
- [ ] Recall/切换时 **整 panel 禁用** 行为与文案。  
- [ ] 上传：选文件 → 确认/替换/取消 流程。  
- [ ] Lead Time / Last N：要么删掉单看，要么和上周「两指标一起」方案对齐。  

### P2 — 排期与文档

- [ ] 6 月底前：三页面 + 基本逻辑；simulation 占位即可。  
- [ ] 把你整理的逻辑文档给老师过目。  

---

## 八、与老师沟通原话片段（便于回溯）

- 「先把样式全部都定下来，再改问题。」  
- 「布局都没弄好就又想搞颜色，可能会顾此失彼。」  
- 「如无必要勿增实体。」  
- 「上面是这个样子，下面也应该是这样——宽、字体。」  
- 「Lead time 说实话没什么意义，两根线看 completion 和 scrap 还可以。」  
- 「Checkout 之后应该走了，为什么一直绿到下一轮？」  
- 「初始化应该开始就做了，跑的过程中只是填数据。」  

---

*文档版本：本周面谈整理 · 文件名 `优化2.md`*
