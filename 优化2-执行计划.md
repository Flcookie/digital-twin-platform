# 优化 2 · 执行计划

> 配套文档：`优化2.md`（老师反馈原文）  
> 目标：**6 月底前** 三页面（Home · KPI · Digital Twin）**样式定版 + 基本逻辑可点不穿**；simulation 仅占位。  
> 原则：**先样式、后逻辑**；**先布局、后配色**；每阶段结束 **自己录屏 30 秒给老师看**，避免下次又混谈。

---

## 约束：先定版式 · 不增不改老

### 必须遵守

| 规则 | 说明 |
|------|------|
| **先定版式** | 布局 → 各级标题字号 → 颜色；**本节「版式定稿」定稿前不写业务逻辑** |
| **只在现有界面上优化** | 改展示与排版，不重做产品形态 |
| **不增加老师未提的东西** | 无新页面、新 KPI、第三张趋势图、Simulation 实现、KPI→log 联动、hash 校验、合并 03/04 页等 |
| **不动老东西** | 见下表「禁止改动」 |

### 本轮只动（样式阶段）

| 允许改 | 禁止改 |
|--------|--------|
| `code/ui_theme.py`（**只扩展常量/CSS，不拆新文件**） | `kpi_calculator.py`、`mqtt_backend.py`、`neo4j_backend.py` |
| `streamlit_app/ui_kpi_display.py` | `main_service`、监控 `code/monitoring*` |
| `streamlit_app/ui_home.py` | `pages/02_History.py`、`03_Part_Trace.py`、`04_Conformance.py`（独立旧页） |
| `streamlit_app/ui_sidebar.py`（全站 h1–h3、容器宽，与定稿对齐） | `process_control` 行为（Recall 逻辑放 Phase 4） |
| `streamlit_app/ui_part_trace_panel.py`、`pages/05_Digital_Twin.py`、`factory_floor_plotly.py`（仅老师点名的样式） | 新 Tab、新路由、新数据库字段 |

### 页面范围（三页）

1. **Home** — `app.py` → `ui_home.render()`  
2. **KPI** — `pages/01_Realtime.py` → `ui_kpi_display.render_kpi_dashboard()`  
3. **Digital Twin** — `pages/05_Digital_Twin.py` + 内嵌 `ui_part_trace_panel`

### 修订对照（你提的三点 · 已写入下文）

| 你的判断 | 结论 |
|----------|------|
| 状态色 Busy 绿 / Idle 灰 | **采纳**；初稿橘/绿来自转写歧义，已改「版式定稿 · D」 |
| Lead Time 不要改成 Avg Flow Time | **采纳**；只保留现 KPI 名，允许 AVG、去 Rate/Over Time 等展示微调 |
| Phase 0 用独立常量、不用 `UI_SPEC` dict | **采纳**；见「版式定稿 · G」 |
| Stage 列顺序样式阶段不动 | **采纳**；见 C.1 |
| 趋势图保留小图例 | **采纳**；见 E（非 `showlegend=False`） |
| `STATUS_COLORS` 键名 `BLOCKED` | **采纳**；`ui_theme` 现 `BLOCK` 在 Phase 0 改为 `BLOCKED` |
| Trace 图标方案写死 | **采纳**；见 Phase 3 任务 4 |
| 每 Phase 用 git diff 验收 | **采纳**；见「执行护栏」 |

### 执行护栏（每个 Phase 开始前 / 结束时必做）

> **最大风险**：样式与逻辑混改。用 git 约束执行范围。

| 时机 | 动作 |
|------|------|
| **Phase 开始前** | `git stash push -m "pre-phase-N"`（或分支 `style/phase-N`） |
| **Phase 结束时** | `git diff --name-only` 对照下表；出现禁止文件则回滚 |
| **见老师前** | 第十一章自测 + 本表 diff |

| Phase | `git diff` 仅允许 |
|-------|-------------------|
| P0 | `code/ui_theme.py`，`streamlit_app/ui_kpi_display.py`（仅 import / 删 `_UI_THEME`） |
| P1 | `ui_kpi_display.py`，`ui_sidebar.py`（仅字号/CSS） |
| P2 | `ui_home.py`，`ui_theme.py`（仅 Home 用 CSS） |
| P3 | `ui_part_trace_panel.py`，`pages/05_Digital_Twin.py`，`factory_floor_plotly.py` |
| P4 | 第十二章 P4 行；**禁止** 样式文件大改配色/布局 |

### Phase 0 产出方式

- **不新建** `ui_tokens.py` 或 `docs/ui_style_guide.md`（避免扩散 scope）  
- 在 **`code/ui_theme.py` 顶部**增加**独立模块常量**（见版式定稿 · G），`ui_kpi_display` 删除重复 `_UI_THEME` 后 **import**  
- 版式数值以 **下文「版式定稿」** 为准；改代码时只落实定稿，不临时发挥

---

## 版式定稿（基于当前项目 · 代码实施前以此为准）

> 下列数值来自现有 `ui_sidebar.py`、`ui_kpi_display.py`、`ui_home.py`、`code/ui_theme.py` 的**收敛与统一**，不是新设计系统。  
> 字体族保持现状：**Barlow Condensed**（正文）+ **Share Tech Mono**（仅日志/等宽，Home 日志弹窗沿用）。

### A. 全局壳层（三页共用）

| 项 | 定稿值 | 现状来源 |
|----|--------|----------|
| 主容器最大宽 | `1440px` | `ui_sidebar._GLOBAL_APP_FONT_CSS` |
| 根字号 | `17px`（`html`） | 同上 |
| 页面 `st.title`（L0） | `2.05rem`，约 **35px**，字重 700，色 `#1e293b` | `ui_sidebar` h1 |
| 页面 `st.subheader`（L0b） | `1.7rem`，约 **29px**，字重 600 | `ui_sidebar` h2 |
| 主区内边距 | 左右 `2rem`，上 `1rem` | `ui_sidebar` |
| 背景 | `#f8fafc` | `ui_theme :root --bg` |
| 卡片面 | `#ffffff`，描边 `#e2e8f0`，圆角 `12px`（大区块）/ `8px`（小卡） | KPI trends wrap / System 卡 |

### B. 标题层级（字号 · 字重 · 颜色）

统一 **5 级**，全站只许用下表，禁止再发明第六种标题样式。

| 级别 | 用途 | 字号 | 字重 | 颜色 | 装饰 | 使用位置 |
|------|------|------|------|------|------|----------|
| **L0** | 页面名 | `2.05rem` | 700 | `#1e293b` | 无 | `st.title`（三页顶部） |
| **L1** | 大区块 | `24px` / `1.41rem` | 700 | `#1e293b` | 左竖条 `4px`，色见下表 | KPI **System KPI**；可选 Trends 外框标题 |
| **L2** | 子区块 | `20px` / `1.18rem` | 700 | `#1e293b` | 左竖条 `4px` | KPI **Trends / Stage / Station**；Home `section_title` **改为 20px、取消全大写**（与 KPI 对齐） |
| **L3** | 指标名 / 表头 | `14px` | 600 | `#64748b` | 无 | 卡内「WIP (pcs)」、Stage 五列标签、Trace 表头 |
| **L4** | 主数值 | `28px`（Stage 内）/ `30px`（System 双行卡） | 700 | 语义色或 `#1e293b` | 无 | System/Stage 数字 |
| **L5** | 说明 / caption | `12–13px` | 400–500 | `#64748b` / `#9e9e9e` | 无 | `st.caption`、Replay 提示、轴旁辅助 |

**L1 / L2 左边条颜色（KPI 与 Home 统一语义）：**

| 区块 | 左边条色 |
|------|----------|
| System / 主强调 / Trends | `#0284c7`（`--accent`） |
| Stage / 中性结构 | `#64748b`（`--text-dim`） |
| Station | `#cbd5e1`（`--border2`） |
| Home · Data Source / Navigate | `#64748b` |
| Home · Live | `#0284c7`（与 accent 一致，**不再单独一套蓝**） |
| Home · History | `#e67700`（保留回放区分，仅此一处橙色标题） |

**图表标题（Plotly，等同 L2 但不画左边条）：**  
- 格式：`{名称} ({单位})`，字号 **18px**，色 `#1e293b`  
- 禁止后缀：`Over Time`、`over time`  
- 定稿标题：`WIP (pcs)`、`Completion & Scrap (%)`

### C. 布局定稿（区块顺序与栅格）

#### C.1 KPI Dashboard（`render_kpi_dashboard`）

```
┌─ container border (kpi_all_panel) ─────────────────────────────┐
│ L1 System KPI                                                   │
│   [卡1 WIP|AVG] [卡2 Comp|Comp%] [卡3 Scrap|Scrap%] [卡4 Flow] │  ← 4 列等宽 gap=small
│ ─── hr #e2e8f0 ───                                              │
│ L2 Trends（wrap：顶边 3px accent + padding 16×20）               │
│   [Plot WIP]  |  [Plot Completion & Scrap]                      │  ← 2 列 50% : 50%
│ ─── hr ───                                                      │
│ L2 Stage KPI (左 50%)  |  L2 Station KPI (右 50%)               │  ← **目标**：`st.columns(2)`；现状为上下堆叠，Phase 1 改为并排
│   Stage 1–3 左栏          State 条 + 4 条对比图                  │
│   Stage 4–6 左栏          （Station 占满右栏高度）               │
└─────────────────────────────────────────────────────────────────┘
```

| 元素 | 定稿尺寸 |
|------|----------|
| System 双行卡 | 高 **162px**（保持），四卡 **同高同宽** |
| System 第 4 卡 | **保持 Lead Time (s) / Lead Time all (s)**（`avg_cycle_time_*`）；仅样式与同高；老师意见是「别单独为 Lead Time 做趋势折线」，不是改名 |
| Trends 图 | 高 **260px**，margin `t36 b30 l50 r20`，两图 **完全相同** layout dict |
| Stage 单卡 | 去掉 **按 stage 序号换色的左边框 3px**；统一边框 `#e2e8f0`；Looping 行背景 `#f1f5f9`（保留） |
| Stage 内列顺序 | **保持代码现状，样式阶段不调顺序**（避免牵动字段映射）；Phase 4 再对照 `kpi_calculator` / `render_stage_kpi_group` 确认是否与老师「WIP → AVG WIP → Flow time → …」一致 |
| Station State 条 | 标签 **State**（已是）；9 站 `grid` 等分 |
| 区块间距 | System→Trends `8px`；Trends 底 `20px`；Trends→Stage/Station `10px` hr |

#### C.2 Home · Control（`ui_home.render`）

```
L0（由 app 页 title 决定，home 内可不重复大标题）
├─ L2 Data Source          [Live 卡 | History 卡]     2 列等宽
├─ L2 Live（border 容器）
│    ├─ status 右对齐（Programs / System 点状条）
│    ├─ sub L3 CONTROL
│    └─ 按钮行 5 列：[1.3,1.3,1.2,1.3,1.0] → **定稿改为等宽 5 列 [1,1,1,1,1]**，min-height 一致（如 40px）
├─ L2 History（border）
├─ L2 Navigate             [KPI 入口 | Twin 入口]     2 列
└─ L2 Other Services
     Deploy 行：**定稿 [1, 1, 1]** — Upload Code | Upload Config | 留白/状态，三列同宽（老师：上传约占 1/3，与上排视觉对齐）
```

| 元素 | 定稿 |
|------|------|
| `section_title` | 高 **20px** 字、**sentence case**（如 `Data source` 或保持 `Data Source`），**不要** `letter-spacing:.07em` 过大全大写 |
| 整块禁用 | 已有 `home_live_ops_block` 的 `pointer-events:none` + `opacity:0.48` → **复用到** Data Source 切换、Recall 时 **整 panel**（Phase 2 仅扩 class，不改后台） |
| Dashboard 入口按钮 | 与 CONTROL 按钮 **同 min-height、同圆角 6–8px** |

#### C.3 Digital Twin（`05_Digital_Twin.py`）

```
L0 Digital Twin
L0b Factory layout（原 subheader，字号跟 L0b = 1.7rem）
├─ 地图 container（全宽，displayModeBar: false）
├─ divider（#e2e8f0）
└─ L2 Part trace（border container，样式对齐 KPI trends wrap：顶边 3px accent）
      └─ 矩阵 + Complete trace（宽度 min-width 100%，横向 scroll 允许）
```

| 元素 | 定稿 |
|------|------|
| 区块分割 | 与 KPI **同一套** wrap CSS（从 `ui_theme` 引用 class 名，不复制第三套） |
| Trace 表头 | L3 **14px** `#64748b`；表头底 `#2c3e50` **改为** `#1e293b` 或 `#334155`（与全站 text 一致，**去掉单独深蓝风格**） |
| Live part 轨迹点 | **红色** `#ef4444` / `#dc2626`（老师：live 用红，history/完成用绿 `#1a6b3c`、灰 `#d1d5db`） |

### D. 颜色定稿（语义色板）

**结构色（`ui_theme.py` :root，保持）：**

| Token | 色值 |
|-------|------|
| `--bg` | `#f8fafc` |
| `--surface` | `#ffffff` |
| `--surface2` | `#f1f5f9` |
| `--border` | `#e2e8f0` |
| `--border2` | `#cbd5e1` |
| `--text` | `#1e293b` |
| `--text-dim` | `#64748b` |
| `--accent` | `#0284c7` |

**工站 / 状态（与老师语义一致；写入 `STATUS_COLORS` 与 `ui_kpi_display` _station_* 同一源）：**

> **修订说明：** 初稿误将 Busy=橘、Idle=绿（受转写「黄/橘=busy、idle=绿」影响）。按老师语义与现场含义应为：**Busy=正在干活（好事）→ 绿；Idle=不动 → 灰**。实施时替换现 `ui_kpi_display` 注释「Busy=黄 / Idle=绿」及 `_STATION_*` 色值。

| 状态 | 定稿色 | 说明 |
|------|--------|------|
| **Busy** | `#22c55e` | 绿（正常工作） |
| **Idle** | `#94a3b8` | 灰（不动 / 空闲） |
| **Blocked** | `#eab308` | 黄 |
| **Fail** | `#ef4444` | 红 |
| **Log / 静止** | `#94a3b8` | 灰（与 Idle 同色即可；若需区分再用 `#cbd5e1`） |

`code/ui_theme.STATUS_COLORS` 现状 BUSY=蓝 `#3b82f6` → 实施时改为上表，与 monitoring 展示统一。

**KPI 语义（数字与折线，不新增含义）：**

| 用途 | 色值 |
|------|------|
| WIP 数字 / 线 | `#1565c0`（或 `#0284c7` accent，**二选一，定稿用 accent `#0284c7`**） |
| AVG WIP 虚线 | `#f97316`（趋势辅助线，**不**占用 Busy/Idle 状态色） |
| Completion 线 | `#16a34a` |
| Scrap 线 | `#dc2626` |
| Completions 数字 | `#2e7d32` → 可保留或改为 `#16a34a` 与线一致 |
| Scraps 数字 | `#c62828` → 与 `#dc2626` 统一 |

**删除或弱化的颜色（老师：stage 五色边框无用）：**

- Stage 2/4/6 紫、Stage 1/3/5 蓝 **左边框强调** → 取消，仅 Looping 灰底区分  
- 不为 DECO 新增蓝条、金条、indigo 装饰

### E. 图表与轴（KPI Trends）

| 项 | 定稿 |
|----|------|
| Y 轴 WIP | **从 0 起**，`rangemode` 不截断负值 |
| Y 轴 Completion & Scrap | **固定 [0, 100]**，tick `0,25,50,75,100`；单位只在标题 `%`，轴旁 **不重复** `Rate` |
| 图例 | **`showlegend=True`**，小图例放 **图内右上角**（`x=0.99, y=0.99, xanchor=right`, 字号 12px）；不挡曲线。老师说的是去掉图**中间**多余 marker/大色块，不是让用户猜线色 |
| hover | 时间 + 线名 + 数值；**去掉 Sample N**（hover 与图例互补） |
| 竖线 / 日期标注 | **无** |
| 字体 | 与 `_PLOT_FONT` 一致：Barlow Condensed 15px 轴、18px 图标题 |

### F. 指标文案定稿（展示层微调 · **不改 KPI 定义与字段名**）

> **原则（与老师 + 代码一致）：**  
> - **Lead Time** 与 **Flow Time / avg_flow_time** 在 `kpi_calculator` 里是不同概念（如 `avg_cycle_time_*` vs `stage.avg_flow_time`），**不得**把界面上的 Lead Time 统一改名为 Avg Flow Time。  
> - 老师说的是：**单独 Lead Time 折线意义不大**（趋势区用 Completion + Scrap 双线即可），不是让你改指标定义。  
> - 允许：老师点名的 **average → AVG**、去掉多余 Rate/Over Time、单位括号统一。

| 允许改的展示 | 定稿 | 不改 |
|--------------|------|------|
| Average WIP / 平均 WIP | **AVG WIP (pcs)**（已有） | 字段 `wip_average` / `avg_wip` |
| Completion Rate (%) | **Completion (%)**（可选去掉 Rate 字样） | 字段 `yield_rate` |
| Scrap Rate (%) | **Scrap (%)** | 字段 `scrap_rate` |
| Idle (%) 等 | **Idle fraction (%)** 或保留 **Idle (%)** | 字段 `state_probability` |
| WIP | **WIP (pcs)** | 全站 **pcs** |
| Runtime | 只用 **Runtime**，不用 Observation time | — |
| **Lead Time (s)** / **Lead Time all (s)** | **保持现名**（System 卡，对应 `avg_cycle_time_*`） | 不改成 Avg Flow Time |
| Stage 列「Lead Time (s)」 | **保持现名**（UI 已绑定 `avg_flow_time` 字段；本轮**只改样式不改标签**，避免与 calculator 语义再搅乱；若日后改标签需单独对照 `kpi_calculator` 文档） | 不在样式阶段改字段映射 |

### G. Phase 0 写入 `ui_theme.py`：仅用独立常量（不用 `UI_SPEC` dict）

```python
# 实施时照抄 — 全部模块级常量，不混用 dict 包一层
FONT_L0_REM = 2.05
FONT_L1_PX = 24
FONT_L2_PX = 20
FONT_L3_PX = 14
FONT_L4_PX = 28
FONT_L5_PX = 12

CARD_SYSTEM_H_PX = 162
CHART_TREND_H_PX = 260
LAYOUT_MAX_WIDTH_PX = 1440

CHART_TITLE_WIP = "WIP (pcs)"
CHART_TITLE_RATES = "Completion & Scrap (%)"

STATUS_COLORS = {
    "IDLE": "#94a3b8",
    "BUSY": "#22c55e",
    "BLOCKED": "#eab308",   # 与 kpi_calculator / Station live 一致；勿写 BLOCK
    "FAIL": "#ef4444",
}
# STATUS_EMOJI 键名同步为 BLOCKED（Phase 0 替换 ui_theme 里原 BLOCK）
```

`ui_kpi_display._UI_THEME` → 删除重复 dict，改为 `from ui_theme import STATUS_COLORS, FONT_L2_PX, ...`（经 `paths.ensure_paths`）。

---

## 一、现状速览（2025-05 代码对照）

| 项 | 现状 | 与老师差距 |
|----|------|------------|
| 双线趋势图 | `ui_kpi_display._fig_completion_scrap_over_time` 已有 | 标题仍带 “Over Time”；Y 轴 0–100 已有；需去 Sample/多余 hover |
| WIP 趋势 | `_fig_wip_over_time` 已有 | 标题格式统一为 `WIP (count)`；与右图等高/同 margin |
| System 四格卡 | WIP/Completion/Scrap/**Lead Time** | **保留 Lead Time 文案**；趋势区不做 Lead Time 折线即可 |
| Stage 卡 | 五色左边框 + 5 列指标 | 颜色“没用”可减；**样式阶段不改** Stage 列指标名称 |
| 状态色 | `STATUS_COLORS` BUSY=蓝、键名 `BLOCK`；`_STATION_*` 黄/绿与老师相反 | 定稿：**Busy 绿、Idle 灰、Blocked 黄、Fail 红**；键名 **BLOCKED**（见版式 D/G） |
| Checkout 闪绿 | `_PT_FLASH_SEC=2` 已有 | 老师见“一直绿到下一轮”→ 查 `lap_open`/行背景逻辑是否覆盖 flash |
| Home 禁用 | 各按钮 `disabled=` | 需 **整块 container** CSS `pointer-events:none` + 半透明遮罩 |
| 上传 | `st.file_uploader` 直传 | 需 **Select → Preview/Confirm → Upload** 两步 |
| 主题集中 | `_UI_THEME` 与 `ui_theme` 两套 | **只扩展 `ui_theme.py`**，`ui_kpi_display` 改为 import（见「版式定稿」） |

---

## 二、分阶段路线图（建议 4 周，可按考试压缩）

```
Week A（2–3 天）  落实「版式定稿」到 ui_theme + KPI 页样式定版
Week B（2–3 天）  Home 控制区样式 + 整块禁用
Week C（2–3 天）  Digital Twin / Trace 样式 + hover
Week D（3–5 天）  逻辑修复（checkout、排序、上传、recall）
缓冲（持续）      模糊项文档化 + 老师过一遍逻辑说明
```

**你每天的最小闭环：** 改 1 个文件 → `streamlit run` 刷新 → 截图对比 → 勾 checklist。

---

## 三、Phase 0：设计基座（半天，必做）

**目的：** 把上文 **「版式定稿」** 写进 `code/ui_theme.py`，后续只引用常量，不在各文件硬编码颜色字号。

### 任务（仅扩展 `ui_theme.py`，不新建文件）

1. 在 `code/ui_theme.py` 顶部增加 **独立常量**（见「版式定稿 · G」），**不使用 `UI_SPEC` dict**。  
2. 将 **`STATUS_COLORS`** 按「版式定稿 · D」：**Busy 绿 / Idle 灰 / Blocked 黄 / Fail 红**；键 **`BLOCK` → `BLOCKED`**（与 `kpi_calculator` 一致）。  
3. `ui_kpi_display`：删除 `_UI_THEME`，改为从 `ui_theme` import；`_KPI_SECTION_TITLE_CSS` 字号与 L1/L2 表对齐。  
4. `ui_sidebar`：根字号、`h1`–`h3` 与 L0/L0b 表对齐（数值已一致则加注释引用 `ui_theme.FONT_*` 常量）。  
5. **不改** 任何页面路由、KPI 计算、MQTT 逻辑。

### 完成标准

- [ ] 「版式定稿」A–G 表内数值，在 `ui_theme.py` 均可找到对应常量  
- [ ] `ui_kpi_display` 无独立 `_UI_THEME` 重复 dict  
- [ ] 单位全站 **pcs / % / s** 三种，指标文案见「版式定稿 · F」  
- [ ] **`git diff` 仅含 P0 允许文件**（见「执行护栏」）

---

## 四、Phase 1：KPI Dashboard 样式定版（Week A）

**主文件：** `streamlit_app/ui_kpi_display.py`  
**页面：** `pages/01_Realtime.py`（只调 `render_kpi_dashboard` 参数，不动逻辑）

### 1.1 布局（第 1 天）

| # | 改什么 | 怎么做 |
|---|--------|--------|
| 1 | Section 顺序 | System 卡 → Trends（横线分割）→ Stage \| Station 并排 |
| 2 | Trends 两图 | `st.columns(2)` 等宽；`height`/`margin` 用 tokens 完全一致 |
| 3 | System 四卡 | 四卡 **同高 162px、等宽**；col4 **保留 Lead Time (s)** 现名（见版式定稿 F） |
| 4 | Stage \| Station | `st.columns(2)` 并排；**Stage 五列顺序不动**（见 C.1） |

### 1.2 图表规范（第 2 天）

| # | 改什么 | 怎么做 |
|---|--------|--------|
| 5 | 标题 | 用 `ui_theme.CHART_TITLE`：`WIP (pcs)`、`Completion & Scrap (%)`，去掉 “Over Time” |
| 6 | Y 轴 | WIP 从 0 起；率图 0–100，轴旁标 `%` 或 `%` 在标题、轴 tick 只数字 |
| 7 | 图例 | 小图例右上角（见版式 E）；hover 保留时间 + 线名 + 值 |
| 8 | 竖线/注记 | 删 `add_vline`、Sample 序号、图中间 marker |
| 9 | 对称 | `_trend_layout_common` 两图共用；legend/hover 配置一致 |

### 1.3 Station 区（第 2 天下午）

**文件：** 同文件 `render_station_kpi_group` 一带  

| # | 改什么 |
|---|--------|
| 10 | `Current` → **State** |
| 11 | `probability` / `rate` → **Idle fraction (%)** 或 **Utilization (%)**（与 `kpi_calculator` 字段对齐后定名） |
| 12 | 状态条颜色 = `STATUS_UI`；考虑 **去掉 stage 左边框五色**，只保留 Looping 灰底行 |

### 1.4 验收（Phase 1 Done）

- [ ] **未连接 MQTT** 打开 KPI 页：布局与 **连接后** 一致（空图占位，不跳样式）  
- [ ] 截图：空态 / 回放 / 实时 三张对比，字体宽度一致  
- [ ] 老师 P0 清单中 KPI 相关 6 条全勾  
- [ ] **`git diff` 仅含 P1 允许文件**；`kpi_calculator.py` / `mqtt_backend.py` / `neo4j_backend.py` 无改动  

**本阶段不改：** `kpi_calculator` 算法、Neo4j 查询、MQTT 订阅；**Stage 列顺序与字段映射**。

---

## 五、Phase 2：Home 控制区样式（Week B）

**主文件：** `streamlit_app/ui_home.py`  
**参考：** 老师不要大表格 → 保持卡片条，但 **对齐 + 整块禁用**

### 2.1 布局

| # | 任务 |
|---|------|
| 1 | Deploy 行改为 **三列等宽** `[1,1,1]`（版式定稿 C.2）；CONTROL 五按钮改为 **五列等宽** |
| 2 | 所有主按钮 **同 min-height（40px）**、同圆角；间距与版式定稿一致 |
| 3 | 减少 “三种钥匙/三种按钮风格”；Live/Local 卡片用同一 `cp-card` class |

### 2.2 整块 Decouple（样式层）

```text
recall / 切换数据源 / replay 未就绪
  → 给 .cp-ops-panel 加 class cp-panel--locked
  → CSS: opacity + pointer-events:none（整块）
  → 不要 8 个 button 各自 disabled 不同灰度
```

实现：`_render_*` 外包 `<div class="cp-panel-wrap cp-panel--locked">` + 一段共享 CSS（可放 `ui_home.py` 底部或 `ui_theme.THEME_CSS`）。

### 2.3 上传 UI（仅布局，逻辑放 Phase 4）

| 步骤 | UI |
|------|-----|
| 1 | `file_uploader` label 改为 **Select file** |
| 2 | 选中后显示文件名 + **[Upload]** **[Cancel]** 两按钮（Cancel 清 session） |
| 3 | Upload 按钮 `disabled` 直到用户点 Confirm |

### 2.4 验收

- [ ] 点切换 Live→History：整个 Physical 区变灰，不是单键灰  
- [ ] Upload 区高度与左列一行对齐  
- [ ] Dashboard 入口 KPI/Twin 按钮样式与主控区一致  
- [ ] **`git diff` 仅含 P2 允许文件**

---

## 六、Phase 3：Digital Twin / Trace 样式（Week C）

**主文件：**  
- `pages/05_Digital_Twin.py`  
- `ui_part_trace_panel.py`  
- `factory_floor_plotly.py`（live 轨迹色）

| # | 任务 | 文件提示 |
|---|------|----------|
| 1 | 区块标题：`Factory Layout` / **Part Trace** / **Past Trace**（缩短） | `05_Digital_Twin.py` |
| 2 | Layout 与 Trace 间 **divider + trends wrap 同款 CSS**（class 放 `ui_theme.THEME_CSS`，不新建组件文件） |
| 3 | Trace 表 **加宽** 或 `overflow-x:auto`；hover 显示 Part ID + 当前站 | `ui_part_trace_panel` HTML title |
| 4 | **Trace 格内图标统一**（见下框）；删 emoji / SVG / 文字混用 | `_twin_flow_slot_cell_inner_html`、图例 HTML |
| 5 | Live 轨迹色：**红**；History/完成：**绿/灰** | `factory_floor_plotly` |
| 6 | 图例缩小；地图上不显示占大面积的无意义色块 | plotly layout |
| 7 | 页面加载时 **先 render 空表骨架**（与 KPI 空态同理） | `render_digital_twin_flow_conformance_table([])` |

**任务 4 · 图标定稿（不引入新图标包）：**

```text
优先：Unicode 符号 + 与 STATUS_COLORS 一致的色块（与 ui_theme.STATUS_EMOJI 对齐）
  · Processed / DONE  → 实心小方块（绿底 #1a6b3c），不用随机 emoji
  · Rework            → ↺（U+21BA），橙底 #e67e22
  · Scrap             → ×（U+00D7）或统一 ✗，灰底；全表只选一种
  · Not done          → 浅灰空框 #e8e8e8
禁止：emoji 脸、外链 SVG、Material 与 emoji 混排、三套图标并存
可选：格内仅引用 ui_theme.STATUS_EMOJI[...]，不另起一套
```

### 验收

- [ ] 鼠标悬停 Trace 单元格 3 秒内能看到 Part/站信息  
- [ ] 未跑系统时打开 DT 页，不出现 “跑起来才变样”  
- [ ] Trace 格内图标 **同一套规则**（见上框）  
- [ ] **`git diff` 仅含 P3 允许文件**

---

## 七、Phase 4：逻辑修复（Week D，样式冻结后）

| 优先级 | 问题 | 调查 / 修改 |
|--------|------|-------------|
| P1 | Checkout 一直绿 | `ui_part_trace_panel._update_part_trace_row_flashes`：`lap_open` 为 true 时是否仍用绿色底；checkout 后是否应从列表移除或只 flash 2s |
| P1 | Pat 进来顺序乱 | 列表 sort key：checkout 时间或 part_id；新行 **insert 规则** 写进 `part_track_conformance` |
| P1 | Recall 灰一下 | `process_control` 读文件时设 `st.session_state.cp_busy=True`，fragment 显示 “Preparing…” |
| P2 | 上传确认 | `ui_home`：仅 Confirm 后调现有 upload API；Cancel 清 `_lcp/_lcf` |
| P2 | Last N | 无定义则 **删标签** 或 tooltip；**Lead Time 指标名不在 P2 改名**（与 Flow Time 区分） |
| P3 | KPI 点选联动 log | 单独小任务：点击 station 卡 → sidebar 过滤 log（老师觉得现在看不出来） |

**规则：** Phase 4 只允许改逻辑文件；若发现要改颜色，记到 backlog **下一版样式**，本阶段不碰。

### 完成标准

- [ ] Checkout / 排序 / 上传 / Recall 按上表 P1→P2 验收  
- [ ] **`git diff` 无** `ui_kpi_display.py` / `ui_home.py` 的 **CSS、字号、配色** 大段改动（仅允许逻辑相关小改，如 `disabled` 条件）  
- [ ] Stage 列顺序 / 标签与 `kpi_calculator` 对照（若做则在本阶段文档化，不单改样式）

---

## 八、模糊项处理（并行，别阻塞样式）

| 模糊项 | 你怎么 closure |
|--------|----------------|
| 上周「五」、跑起来显示 | 翻 `week5.md` / git log；问老师带 **截图** |
| 每圈完成时间 | 查 event_log 里 FINISH 间隔；是否在 Trace 弹窗显示 lap duration |
| 两根线 vs 三根线 | **定稿：保留 WIP + Completion&Scrap 两图**；stage 不做第三张折线 |
| Transfer 是否进 Past trace | 写 3 行 product note 给老师确认 |
| Simulation | `app.py` 入口 **Coming soon** 灰块，不写逻辑 |

---

## 九、6 月底里程碑（与老师预期对齐）

| 里程碑 | 内容 | 建议完成日 |
|--------|------|------------|
| M1 | KPI 页样式冻结 | Week A 末 |
| M2 | Home + DT 样式冻结 | Week C 末 |
| M3 | 三页基本逻辑（Start/Stop/Replay/切换不出错） | 6 月中旬前（留考试周） |
| M4 | 逻辑说明 1 页纸 + 演示脚本 | 见老师前一天 |

**考试策略（6 月中旬两场）：**  
- 5 月底前完成 M1–M2  
- 考试周只修 bug，不加新功能  
- 6 月下半月 M3 + 老师 review

---

## 十、推荐工作顺序（明天早上给老师看进展）

```text
Day 1  上午：Phase 0 — 只改 ui_theme.py + ui_kpi_display import（落实版式定稿 G）
       下午：KPI 趋势图标题/轴/图例（1.2 的 5–9）
Day 2  上午：System/Stage 卡布局与命名（1.1 + 1.3）
       下午：空态与连接态一致（验收 1.4）
Day 3  Home 布局 + 整块 decouple（Phase 2）
Day 4  DT Trace 宽度 + hover + 标题（Phase 3）
Day 5+ Phase 4 逻辑（按 P1→P2）
```

---

## 十一、自测清单（每次见老师前 5 分钟）

1. 冷启动打开三页，样式是否一致？  
2. KPI 趋势是否 **双线都在**、Y 从 0、**右上角小图例可辨 Completion / Scrap**？  
3. 切换数据源时是否 **整 panel 灰**？  
4. Trace hover 能否看到 Part？  
5. Checkout 后 2 秒绿是否消失？  
6. **`git diff --name-only` 是否仅含当前 Phase 允许文件**（与「执行护栏」表一致，不是目测）

---

## 十二、文件改动速查表

| 阶段 | 主要改动文件 |
|------|----------------|
| P0 | **仅** `code/ui_theme.py`（+ `ui_kpi_display` 删重复 `_UI_THEME`） |
| P1 | `streamlit_app/ui_kpi_display.py` |
| P2 | `streamlit_app/ui_home.py` |
| P3 | `ui_part_trace_panel.py`, `05_Digital_Twin.py`, `factory_floor_plotly.py` |
| P4 | `ui_part_trace_panel.py`, `part_track_conformance.py`, `process_control.py`, `ui_home.py` |

---

*与 `优化2.md` 第七章 P0/P1 清单一一对应；完成一项可在 `优化2.md` 里同步打勾。*
