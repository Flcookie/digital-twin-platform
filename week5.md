# 第 5 周工作规划（会议纪要 + 本周任务）

> 依据本周与老师的讨论整理。 
> **技术结论：前端控制台与交互改用 Streamlit；现有 FastAPI + WebSocket 方案对当前团队与维护成本不合适。**

---

## 一、会议要点摘要

### 1. 架构取舍：分布式 / 微服务 vs 单主进程

- 老师强调：当前课题规模不必强行上「前后端分离 + 每个服务一套 REST API」的完整微服务体系；那样扩展性理论上有，但**设计复杂度、部署、API 维护**对单人和低年级同学不友好。
- 更倾向**单一主进程（或少数进程）**：把「服务一、服务二」**内嵌**在同一个主流程里，通过代码调用协作，而不是每个能力都暴露独立 HTTP API。
- 草图含义：**从多 API、多服务盒子 → 一个圈住的「Main Process」**，在简单与可扩展之间找平衡；真要到云上 K8s 级别，需要团队化工程，与本项目阶段不匹配。

### 2. 时钟与时间同步

- 各节点独立时钟，若不同步会导致**事件顺序 / 时间戳**与直觉不符；误差可能到**秒级**，对秒级采样的系统仍会造成「乱序」观感。
- 已通过**协议 + 时间服务器（兼 NTP / MQTT 等）**统一时间；配置侧把「broker」理解为**多用途 Server**（时间 + MQTT 等）更贴切。
- 研究/论文角度：可从「关系只剩一条」的路径、不同聚合视角（系统 / 工位 / 零件）解释数据与 Neo4j 建模，而不只纠结全局绝对时序。

### 3. 数据与 Neo4j：多视角提取

老师建议从多种关系/视角组织或查询事件（Schema 随**服务**可扩展）：

| 视角 | 用途示例 |
|------|-----------|
| **Part** | 追某零件全流程；需清晰 **进入 / 退出**（如 START、FINISH/SCRAP）边界 |
| **Station** | Conform checking 只关心单工位；快速过滤该工位事件 |
| **系统（多站融合）** | 全局行为、多工位事件交织 |
| **Activity / Process** | 以活动类型或「Process 边」组织，便于重复查询、刷新页面时低开销 |
| **Part × Process 联合** | 关注某零件在工艺上的顺序（中间经多站仍属同一 Part 生命周期） |

- **空间换时间**：预建关系、冗余合理字段，降低在线查询复杂度。
- 后续可为 Digital Twin / Process 轨迹单独设计关系类型（如「DT-Process」类边），但不必第一周做完。

### 4. 流程与界面草图（老师手绘）

- **工艺流**：START → P1.LOAD → machine/PROCESS → UNLOAD → TRANSFER → FINISH / SCRAP（可多行并行表示多物料路径）。
- **控制台**：顶部 **Start / Stop**；模块 **KPI**、**Check**（Conform Check）、**Digital Twin**；下方大区域承载主内容。
- **布局意向**：一个**主框架**内切换（主菜单：历史 vs 实时；实时内再分 KPI / Trace 等小标签），避免「点控制在 A 页面、看数要去 B 页面」割裂感；可用侧边栏或顶栏组织。
- **Part 呈现**：以 **Part 生命周期**为主（起始–终止）；全流程 Trace 可能路径很长，列表可分页/默认条数（如 10/100 条）。

### 5. 回放（Replay）与已做事项（口述）

- 有**历史日志回放**、**倍速**相对真实时间的演示需求。
- **Replay 机制理解**：按日志中**事件时间间隔**（下一事件与当前事件的时间差）等待后再发下一事件，以模拟原过程节奏（不一定与美良苗等工具一一对应；是否保留/简化可与老师对一下）。
- 本周已做/讨论：全局时间同步相关配置、**window_ms** 等有微调（如 500ms）；**Server** 命名替代纯「broker」语义等。

### 6. 为何转向 Streamlit

- 老师认为 **FastAPI + WebSocket** 对项目组偏重：要维护大量 API 契约、前后端分工、部署两摊。
- **Streamlit**：后端主动推数可通过 **st.fragment / 轮询 / 会话状态** 等模式简化；页面与逻辑更集中在 Python，**适合单人维护 + 低年级同学迭代**。
- **定调**：**以 Streamlit 作为控制台与可视化主体**；不再把「完整前后端分离 + 每功能一个 REST 节点」作为本期目标。

---

## 二、本周执行清单（定稿 · 本周按此收口）

> 与 `改进.md`（v1.7）一致：**本周内**逐项完成下列勾选；顺序建议「先 A 数据 → 再 B Streamlit → 再 C 删 FastAPI → 最后 D 汇报」。  
> `改进.md` 中 Phase 1～4 是**多周总量**；本周等价于 **Phase 1 打实 + Phase 2 主体可演示 + FastAPI 拆除条件齐备**。B7/B8、A5 若时间紧，**至少占位 + 文档一句话**，不得空白。

### 模块 A — 数据与 Neo4j（优先）

- [ ] **A1** 实时：`main_service` → buffer → Neo4j，固定场景跑通，自查无大量丢事件、异常重复 `DF`、Session 串台。
- [ ] **A2** 回放：若回放进 Neo4j，写清与 A1 **共用 Session 规则**（谁在何时 `start_session`），至少成功一次。
- [ ] **A3** 基于 **`neo4j_writer` 真实 Schema** 写 2～3 条定稿 Cypher（示例：按 Part 查 `DF` 链、按 `Station.sysId` 列事件、按 `Session` 开窗），写入仓库（注释或 `改进.md` 附录）。
- [ ] **A4** `event_buffer.window_ms` 与 KPI/flush 行为 **1～2 段说明**（论文/答辩备用）。
- [ ] **A5**（加分）`DF_PROCESS`：批处理或一次性 Cypher 草稿一版（活动名与老师工艺流对齐），文件入库即可，不要求全自动。

### 模块 B — Streamlit（本周必须可演示）

- [ ] **B1** `streamlit run` 可启动；形态 B 时在 README 写明：`main_service` + Streamlit 两进程。
- [ ] **B2** 导航：**实时**（看 KPI/图）与 **历史**（回放/日志）+ **控制**；**Start/Stop** 至少在**侧栏 Control 或实时页顶栏**之一可用（MQTT 与现逻辑一致）。
- [ ] **B3** 界面显示 **Neo4j 连通状态**（成功/失败信息）。
- [ ] **B4** 控制扩展：在 B2 基础上，从原 `web_api` **再迁移**最常用 2～3 项（如 KPI reset、录制、clear_neo4j、上传 等，按你实际使用选）。
- [ ] **B5** **KPI** 只读展示，与当前数据源一致。
- [ ] **B6** **Trace / Part flow**：Part Id 查询 + 表格；**分页** 10/25/50；查询用 A3 的 Cypher。
- [ ] **B7** **Conformance**：占位页 + 规则说明 1～2 句（可先硬编码）。
- [ ] **B8** **Digital Twin**：极简占位（静态图 + 假数据或「下接事件」说明）。

### 模块 C — 移除 FastAPI（本周收尾）

- [ ] **C1** `web_api` **路由/功能 → Streamlit 位置** 对照表（可与 `改进.md` 清单互证）。
- [ ] **C2** `改进.md`「删除前能力对齐」**全部勾选**后再执行删除。
- [ ] **C3** 删除 `web_api.py`、`static/`、纯 uvicorn 启动脚本；全库 **grep** `fastapi`、`uvicorn`、`/api/`、`/ws/` 清残留。
- [ ] **C4** `requirements.txt` 去掉 Web 栈（删前确认 `httpx` 等）；**bat/README** 改为 Streamlit。

### 模块 D — 汇报与备份

- [ ] **D1** **一页纸**：为何 Streamlit、进程架构简图、Neo4j 多视角一句、未竟项（若有）。
- [ ] **D2** 大删前 **git tag 或备份分支**。

### 建议日序

| 时段 | 侧重 |
|------|------|
| **第 1～2 天** | A1–A4、B1–B3 |
| **第 3～4 天** | B4–B6、C1、A3 与 Trace 联调 |
| **第 5 天** | B7–B8、A5（可选）、C2–C4、D1–D2 |

---

## 三、技术结论（写入规划，供后续开发遵守）

1. **UI 与控制面板：采用 Streamlit 重做**；在能力对齐后**删除** FastAPI、`static/` 与 uvicorn 启动链，**仅保留 Streamlit 作为 Web 界面**（详见 `改进.md`「正式技术决议：移除 FastAPI，全量 Streamlit」）。
2. **后端数据处理**：仍以**主进程**（如 `main_service` + Neo4j）为核心；扩展尽量用**模块/包内调用**，而非先拆一堆微服务 API。
3. **Neo4j 模型**：保持与服务可扩展；优先保证 **Part 生命周期、Station 视角、全局融合** 三类查询能讲清故事，再加深 Process 专用关系。
4. **本周代码策略**：以**本节执行清单**为准推进改代码；大删 FastAPI 前须完成 **C2 对齐勾选** 与 **D2 备份**。

---

## 四、待与老师确认的小问题（可选）

- **Replay**：是否必须保留「按间隔休眠」的精确回放，还是演示可用固定倍速简化。
- **Conform Check**：首版是「人工勾选/记录」还是必须接自动规则。
- **页面数量**：是否坚持**单页多 Tab** 严格在一个框架内，还是可以接受 2～3 个 Streamlit 页面文件 + `st.switch_page`。

---

**与改进方案同步**：同上见 **`改进.md`**；**全量 Streamlit、移除 FastAPI** 见「正式技术决议」；Neo4j 对照见「导师周度要求回顾…」（v1.7 起）。

---

*文档版本：Week 5 执行清单定稿（与 `改进.md` v1.7 对齐）*
